{
  "name": "ibm-ecommerce-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd server && node server.js"
  }
}
